<%= form_tag("/", method: "get") do %>
  <%= label_tag(:q, "Enter a Subreddit (example: ruby) or term:") %>

  <p><%= text_field_tag(:q) %></p>
  <p><%= submit_tag 'Fetch', class: 'btn btn-primary' %></p>

  <% if params[:q].blank? %>
    <% params[:q] = 'all' %>
  <% end %>

<% end %>
